// A TYPE FOR THE ISOPEN OBJECT
export type IsOpenType = {
    linksPopup: boolean;
    editorPopup: boolean;

    actionButtonsPopup: {
        smallScreens: boolean;
        largeScreens: boolean
    }
}